{
  "name": "magic-pet",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "node scripts/build-api.mjs && cd app && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "global-agent": "^4.1.3"
  }
}
