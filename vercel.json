{
  "framework": "vite",
  "buildCommand": "npm install --prefix app && npm run build",
  "outputDirectory": "app/dist",
  "installCommand": "npm install && npm install --prefix app",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
